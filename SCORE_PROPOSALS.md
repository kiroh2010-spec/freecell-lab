# 점수 공식 정리

현재 실제 랭킹에는 **기존 점수 공식 라인을 시간 유리 공식으로 보완한 방식**을 사용한다.

- 런타임 랭킹 버전: `current`
- 주간 키: 기존 `YYYY-MM-DD`
- 서버 저장/비교: `freecell_calculate_score()` / `freecell_submit_score()`의 시간 유리 공식. 알파 테스트는 `mode = alpha`로 최저 시간점수 1500점을 실험하고, 베타/normal은 기존 최저 300점 라인을 유지한다.
- 클라이언트 표시/로컬 랭킹: `calculateScore()`의 시간 유리 공식

아래처럼 현재 사용 공식과 보관 공식을 구분한다.

---

## 1) 기존 점수 공식 — 시간 유리 공식으로 보완 후 사용 중

상태: 현재 사용 중. 기존 `current` 랭킹 라인, 기존 week key, `calculateScore()`/`entry.score` 구조를 유지하고 내부 계산만 시간 유리형으로 보완했다.

현재 적용 공식:

```txt
시간점수 = max(1500, 4000 - max(0, 클리어초 - 120) × 5)  # dev/alpha 테스트
무브점수 = clamp(140 - 무브수, 0, 100)

기본점수 = 시간점수 + 무브점수
최종점수 = max(100, 기본점수 - 되돌리기감점 - 필살기감점) × 레벨배수
```

유지한 기존 구조:

- `RANKING_SCORE_VERSION = 'current'`
- 기존 주간키 `YYYY-MM-DD`
- `calculateScore()` / `entry.score` / `weekly_scores.score`
- 랭킹 정렬: 점수 높은 순, 동점이면 시간 짧은 순, 그다음 무브 적은 순
- 되돌리기/필살기 감점과 레벨 배수

메모:

- 예전 10000점대 내부 계산은 더 이상 쓰지 않는다.
- 다만 새 공식은 별도 개편 랭킹이 아니라 기존 점수 라인을 보완한 것이다.

---

## 2) 개편 점수 공식 — 지금은 쓰지 않음

상태: 보관용. 현재 dev/alpha/beta/server 랭킹에 사용하지 않음.

```txt
클리어점수 = 2000

시간보너스 원점수 =
  5분까지: max(0, 1000 - 클리어초 × 3)
  5분~10분: max(0, 100 - (클리어초 - 300) / 3)
시간보너스 = 시간보너스 원점수 × 1.5

이동보너스 원점수 =
  120무브까지: max(0, 900 - 이동수 × 7)
  120~200무브: max(0, 60 - (이동수 - 120) × 0.75)
이동보너스 = 이동보너스 원점수 × 1.5

되돌리기패널티 = 되돌리기사용수 × 40
필살기패널티 = 필살기사용시 200

최종점수 =
(클리어점수 + 시간보너스 + 이동보너스 - 되돌리기패널티 - 필살기패널티)
× 레벨배수
```

메모:

- 과거 `reform`, `scoreV2`, `YYYY-MM-DD-v2` 실험에 사용했던 공식.
- 점수대는 보기 좋았지만 랭킹/표시/서버 저장값이 혼합되며 혼란을 만들었음.
- 현재 실행 경로에서는 제거했고, 재도입하려면 별도 실험 브랜치/알파에서만 검토한다.
