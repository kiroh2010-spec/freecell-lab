# Freecell 난이도 / 레벨 / 점수 규칙

## 방향

- 레벨과 난이도는 **1LV 한 단계로 통일**한다.
- 내부 기본 코드는 `e1`만 사용한다.
- 기존 저장값 `e2/n1/n2/n3`는 모두 `e1(1LV)`로 흡수한다.
- 사용자 화면에서는 Easy/Normal/Hard 같은 표현을 쓰지 않는다.
- 레벨업 테스트는 현재 dev에서 비활성화한다.

## 난이도 단계

| 표시명 | 한국어 | 내부 코드 | 필요 누적 클리어 | 배수 | 의도 |
| --- | --- | --- | ---: | ---: | --- |
| 1LV | 기본 | e1 | 0 | 1.00x | 모든 플레이어가 동일하게 플레이하는 기본 단계 |

## 레벨업 테스트

현재 dev는 1LV 고정 정책이므로 레벨업 테스트를 열지 않는다.

## 점수 규칙

```txt
기본점수 = 10000 - 이동수 × 5 - 힌트사용수 × 100 + 시간보너스
최종점수 = 기본점수 × 1LV 배수
```

시간은 연속 감점이 아니라 컷오프 보너스다. 오래 걸려도 추가 시간 감점은 없다.

| 클리어 시간 | 보너스 |
| --- | ---: |
| 3분 이내 | +200 |
| 4분 이내 | +100 |
| 5분 이내 | +50 |
| 5분 초과 | +0 |

## 난이도 판단 기준

현재는 완전 solver가 아니라, 딜 결과를 평가해 난이도에 맞지 않으면 다시 셔플하는 휴리스틱 방식을 사용한다.

- 1LV: 초반 저카드/이동 가능 카드가 넉넉한 판. `total <= 6`, 맨 위 A/2/3 최소 3장, 시작 이동 가능 카드 최소 4장 기준.
